# AI_USAGE — 我是怎么用 AI 完成这道题的

## 用了哪些 AI 工具

| 工具 | 用途 |
|---|---|
| **Claude Code** | 主力：架构设计、代码生成、数据清洗逻辑、调试、测试编写、前端组件 |
| **DeepSeek（deepseek-v4-pro）** | 产品内的 AI 问答 LLM（受控工具调用，只做意图识别 + 组织回答） |
| **人工（我自己）** | 方案与口径拍板、数据核对、UI 审美判断、AI 出错的复查与修正 |

> 团队日常用 Claude Code，所以我把 Claude Code 当作协作工程师用，而不是「一句帮我写完」。每个关键决策（口径、脏数据处理、工具边界）都是我定，AI 出代码，我再验证。

## 怎么拆任务（真实 prompt 例子）

我不会说「帮我做一个看板」，而是把任务拆成可验证的小步。例如数据清洗阶段：

> 读取 `data/sales.csv`，先**不要修改数据**，分析并报告数据质量问题：完全重复行、缺失值、日期格式不一致、非数字金额/数量、指向不存在门店/商品的脏外键、负金额/负数量。输出每类问题的具体数量，并给出日期字段的实际取值样例，帮助判断 `DD-MM-YYYY` 是「日-月-年」还是「月-日-年」。

这种 prompt 的关键是：**要求先报告、不要改数据**，且**要求给出样例**来支撑判断，而不是直接让 AI 拍脑袋清洗。

## AI 在哪里出错 / 差点编造数字，我怎么发现、怎么修

**① 金额一致性「误报 25 条不一致」**
AI 第一次分析报告「金额 ≠ 单价×数量」有 25 条。我没有直接采信，而是拆开条件用 SQL 逐项核对：结果 25 = 14 条**负数量** + 11 条**零数量**（都是退货/作废单），而「正数量 + 正金额」的正常销售**一条都不差（0 条）**。这说明数据比想象的干净，也避免了把退货单误判成定价错误。

**② `COUNT(*)` 会算错订单数**
销售表是一笔订单多行商品。AI 最初想用 `COUNT(*)` 算订单数，但我发现 12131 行只有 12051 个 `DISTINCT order_id`——直接 `COUNT(*)` 会把订单数虚高 80。修正为 `COUNT(DISTINCT order_id)`。

**③ 客单价分母被空金额订单拉低**
120 条 `amount` 为空的流水分布在 120 个订单里。若客单价 = 营业额 / 全部订单数，会被拉低（35.69 → 35.34）。改为分母用「至少有一条有效 amount 的订单数」，并把这一口径封装进统一 Service，避免每个接口各自算。

**④ 日期格式 `DD-MM-YYYY` 的歧义**
数据混用三种日期格式。通过抽取 day 字段 > 12 的样例（如 `15-07`、`31-07`）确认是「日-月-年」而非「月-日-年」，再统一解析。

**⑤ 负数量但正金额的退货记录**
发现 14 条 `qty < 0` 但 `amount > 0` 的记录（退货但金额没变负号），是真实的脏数据表示。按「不擅自改数据」原则保留原样，只在质量报告与 README 口径里注明。

**⑥ 前端类型与组件同名导致构建失败**
`StoreCategory` 既是 types.ts 里的类型、又是组件名，vite 构建报 `different imports aliased to same local name`。我给类型加别名 `StoreCategoryRow` 修复——这类问题 AI 不会预判，只能靠构建报错 + 逐条排查。

> 总结：**被 AI 骗没发现才扣分**。所以我对 AI 给出的每一个「数据结论」都回到数据库/SQL 复核，把「AI 说的」变成「我验证过的」。

## 哪些部分我决定自己写

- **数据清洗规则**（`init_db.py` 的解析/去重/外键校验）——要精确控制口径，AI 生成后我逐条核对数字。
- **统一统计口径**（`services/analytics.py`）——这是「数字权威」，必须是我确认过的 SQL。
- **自动化测试**——测试断言的是具体数字，必须我亲自写并跑通。
- **工具边界与 system prompt**——决定 LLM「只能查、不能编」的约束，我定，AI 辅助措辞。

AI 主要负责：重复性的样板代码（FastAPI 路由、Vue 组件、ECharts 配置）、数据体检的初版脚本、文档初稿。
